{
  "project": "code-context-retrieval",
  "created": "2026-03-21",
  "tech_stack": {
    "language": "python",
    "test_framework": "pytest",
    "coverage_tool": "pytest-cov",
    "mutation_tool": "mutmut"
  },
  "quality_gates": {
    "line_coverage_min": 90,
    "branch_coverage_min": 80,
    "mutation_score_min": 80
  },
  "real_test": {
    "marker_pattern": "@pytest.mark.real",
    "mock_patterns": ["Mock", "MagicMock", "patch", "mocker", "monkeypatch.setattr"],
    "test_dir": "tests/"
  },
  "constraints": [
    "CON-001: Supported languages: Java, Python, TypeScript, JavaScript, C, C++",
    "CON-002: Query response time must be < 1 second (p95)",
    "CON-003: System must support >= 1000 QPS",
    "CON-004: MCP protocol is the primary AI agent interface",
    "CON-005: Repository source is any Git repository (public + private via SSH/token)",
    "CON-006: Indexing must be performed offline in a distributed cluster",
    "CON-007: BM25 engine: Elasticsearch",
    "CON-008: Vector engine: Qdrant",
    "CON-009: Code parser: tree-sitter",
    "CON-010: Embedding model: CodeSage-large (1024-dim)",
    "CON-011: Reranker model: bge-reranker-v2-m3",
    "CON-012: Top-K results: 3 (default)",
    "CON-013: Index refresh default: weekly, configurable via cron expression"
  ],
  "assumptions": [
    "ASM-001: Git repositories are accessible from the index cluster (network, credentials)",
    "ASM-002: tree-sitter grammars exist and are stable for all 6 supported languages",
    "ASM-003: CodeSage-large produces consistent 1024-dim vectors across versions",
    "ASM-004: Elasticsearch and Qdrant clusters are provisioned and managed externally",
    "ASM-005: Redis is available for caching; degrade gracefully if unavailable",
    "ASM-006: Primary query traffic comes from AI agents (80%+)"
  ],
  "required_configs": [
    {
      "name": "PostgreSQL URL",
      "type": "env",
      "key": "DATABASE_URL",
      "description": "PostgreSQL connection string for repository metadata, API keys, job tracking",
      "required_by": [1, 2, 3, 4, 16],
      "check_hint": "Set to postgresql+asyncpg://user:pass@localhost:5432/codecontext"
    },
    {
      "name": "Elasticsearch URL",
      "type": "env",
      "key": "ELASTICSEARCH_URL",
      "description": "Elasticsearch cluster URL for BM25 keyword search",
      "required_by": [8, 9, 10, 13, 14, 15, 17, 20],
      "check_hint": "Set to http://localhost:9200"
    },
    {
      "name": "Qdrant URL",
      "type": "env",
      "key": "QDRANT_URL",
      "description": "Qdrant cluster URL for dense vector search",
      "required_by": [7, 9, 13, 14, 15, 17],
      "check_hint": "Set to http://localhost:6333"
    },
    {
      "name": "Redis URL",
      "type": "env",
      "key": "REDIS_URL",
      "description": "Redis instance URL for query caching and rate limiting",
      "required_by": [16, 25],
      "check_hint": "Set to redis://localhost:6379/0"
    },
    {
      "name": "RabbitMQ URL",
      "type": "env",
      "key": "RABBITMQ_URL",
      "description": "RabbitMQ broker URL for Celery task queue",
      "required_by": [4, 21, 22],
      "check_hint": "Set to amqp://guest:guest@localhost:5672//"
    },
    {
      "name": "Repository Clone Path",
      "type": "env",
      "key": "REPO_CLONE_PATH",
      "description": "Base directory for cloned Git repositories",
      "required_by": [4, 5],
      "check_hint": "Set to /tmp/codecontext/repos or a persistent volume path"
    },
    {
      "name": "API Secret Key",
      "type": "env",
      "key": "SECRET_KEY",
      "description": "Secret key for API key hashing and session signing",
      "required_by": [16],
      "check_hint": "Generate with: python -c \"import secrets; print(secrets.token_hex(32))\""
    },
    {
      "name": "Query API Port",
      "type": "env",
      "key": "API_PORT",
      "description": "Port for the FastAPI query service",
      "required_by": [17],
      "check_hint": "Set to 8000 (default)"
    },
    {
      "name": "MCP Server Port",
      "type": "env",
      "key": "MCP_PORT",
      "description": "Port for the MCP server",
      "required_by": [18],
      "check_hint": "Set to 3000 (default)"
    }
  ],
  "features": [
    {
      "id": 1,
      "category": "core",
      "title": "Project Skeleton & CI",
      "description": "Set up project structure with src/indexing, src/query, src/shared packages, pyproject.toml, pytest config, alembic setup, and basic health check endpoint",
      "priority": "high",
      "status": "failing",
      "verification_steps": [
        "Given a fresh clone, when running 'pip install -e .[dev]', then all dependencies install without errors and pytest is available",
        "Given the installed project, when running 'pytest tests/test_skeleton.py', then at least one test passes confirming the project structure exists",
        "Given the project, when importing 'src.shared', 'src.indexing', 'src.query', then all imports succeed without errors",
        "Given alembic config, when running 'alembic check', then it confirms migrations are in sync"
      ],
      "dependencies": []
    },
    {
      "id": 2,
      "category": "core",
      "title": "Data Model & Migrations",
      "description": "Implement SQLAlchemy models for Repository, IndexJob, ApiKey, ApiKeyRepoAccess, QueryLog and create Alembic migration. Implement ES/Qdrant/Redis client wrappers in src/shared/clients/",
      "priority": "high",
      "status": "failing",
      "verification_steps": [
        "Given the data models, when running Alembic migration against a test database, then tables REPOSITORY, INDEX_JOB, API_KEY, API_KEY_REPO_ACCESS, QUERY_LOG are created with correct columns and constraints",
        "Given the Repository model, when creating a record with name='test-repo', url='https://github.com/test/repo', then it persists with a generated UUID id, status='pending', and created_at timestamp",
        "Given the storage clients module, when importing ElasticsearchClient, QdrantClient, RedisClient, then all classes are importable and have connection/health-check methods"
      ],
      "dependencies": [1]
    },
    {
      "id": 3,
      "category": "core",
      "title": "Repository Registration",
      "description": "Implement RepoManager service with register_repo() that validates Git URL, checks for duplicates, creates Repository record, and queues initial indexing job",
      "priority": "high",
      "status": "failing",
      "verification_steps": [
        "Given a valid Git URL 'https://github.com/pallets/flask', when calling register_repo(url), then a Repository record is created with status='pending' and the repository ID is returned",
        "Given an invalid URL 'not-a-url', when calling register_repo(url), then a ValidationError is raised within 2 seconds without creating a record",
        "Given an already-registered URL, when calling register_repo(url) again, then a ConflictError is raised indicating the repository already exists"
      ],
      "dependencies": [2]
    },
    {
      "id": 4,
      "category": "core",
      "title": "Git Clone & Update",
      "description": "Implement GitCloner with clone_or_update() that performs git clone for new repos or git fetch+reset for existing repos, with error handling for network/auth/disk failures",
      "priority": "high",
      "status": "failing",
      "verification_steps": [
        "Given a newly registered repository, when clone_or_update() runs, then the repo is cloned to REPO_CLONE_PATH/{repo_id} and the working copy contains the default branch HEAD",
        "Given a previously cloned repository, when clone_or_update() runs again, then it performs git fetch and resets to latest HEAD without re-cloning",
        "Given a clone operation that fails (unreachable URL), when clone_or_update() runs, then it raises a CloneError with the error message and cleans up partial files"
      ],
      "dependencies": [3]
    },
    {
      "id": 5,
      "category": "core",
      "title": "Content Extraction",
      "description": "Implement ContentExtractor that classifies files by type (source, doc, example, rule) by extension/path patterns, skips binary/oversized files, and reads text content",
      "priority": "high",
      "status": "failing",
      "verification_steps": [
        "Given a cloned repo with .py, .java, .md, and .png files, when extract_content() runs, then source files are classified as 'code', .md files as 'doc', and .png files are skipped",
        "Given a file larger than 1MB, when extraction runs, then the file is skipped with a logged warning",
        "Given files in examples/ directory, when extraction runs, then they are classified as 'example' content type",
        "Given CLAUDE.md, CONTRIBUTING.md, .cursor/rules files, when extraction runs, then they are classified as 'rule' content type"
      ],
      "dependencies": [4]
    },
    {
      "id": 6,
      "category": "core",
      "title": "Code Chunking",
      "description": "Implement Chunker with tree-sitter AST parsing for 6 languages, producing L1 (file), L2 (class), L3 (function) chunks with symbol, signature, doc_comment extraction. Includes DocChunker for markdown heading-based splitting and RuleExtractor for rule files.",
      "priority": "high",
      "status": "failing",
      "verification_steps": [
        "Given a Python file with 1 class containing 3 methods, when chunk_file() runs, then it produces 1 L1 file chunk, 1 L2 class chunk, and 3 L3 function chunks with correct symbol names and signatures",
        "Given a Java file with 2 classes, when chunk_file() runs, then it produces L1, L2, and L3 chunks with parent_class set correctly for methods",
        "Given a README.md with H1, H2, H3 headings, when chunk_markdown() runs, then it produces doc chunks split at H2 level with breadcrumb trails (e.g., 'Getting Started > Installation')",
        "Given a file in an unsupported language, when chunk_file() runs, then it falls back to L1 file-level chunking only"
      ],
      "dependencies": [5]
    },
    {
      "id": 7,
      "category": "core",
      "title": "Embedding Generation",
      "description": "Implement EmbeddingEncoder using CodeSage-large model via sentence-transformers, with encode_documents() for batch indexing and encode_query() with instruction prefix for query-time embedding. Write embeddings to Qdrant.",
      "priority": "high",
      "status": "failing",
      "verification_steps": [
        "Given a list of 10 code chunks, when encode_documents() runs in batch, then it produces 10 vectors of 1024 dimensions each as float32 arrays",
        "Given a query string 'how to configure timeout', when encode_query() runs, then it prepends 'Represent this code search query: ' and returns a single 1024-dim vector",
        "Given 100 chunks with embeddings, when IndexWriter.write_code_chunks() runs, then all 100 vectors are stored in the Qdrant 'code_embeddings' collection with correct payload metadata"
      ],
      "dependencies": [6]
    },
    {
      "id": 8,
      "category": "core",
      "title": "Keyword Retrieval (BM25)",
      "description": "Implement Retriever.bm25_search() using Elasticsearch with the code_analyzer for code_chunks index and doc_analyzer for doc_chunks index, returning top-200 scored candidates",
      "priority": "high",
      "status": "failing",
      "verification_steps": [
        "Given indexed code chunks containing 'getUserName', when bm25_search('getUserName') runs against code_chunks, then results include chunks containing that symbol, ranked by BM25 score",
        "Given the code_analyzer with synonym filter, when searching 'auth', then results also include chunks containing 'authentication' and 'authorization'",
        "Given a query with no matching terms, when bm25_search() runs, then it returns an empty list without error",
        "Given Elasticsearch is unreachable, when bm25_search() runs, then it raises a retrieval error (caller handles degradation)"
      ],
      "dependencies": [7]
    },
    {
      "id": 9,
      "category": "core",
      "title": "Semantic Retrieval (Vector)",
      "description": "Implement Retriever.vector_search() using Qdrant HNSW search with cosine similarity, encoding the query via EmbeddingEncoder.encode_query() and returning top-200 candidates",
      "priority": "high",
      "status": "failing",
      "verification_steps": [
        "Given indexed embeddings for code about HTTP timeouts, when vector_search('how to configure spring http client timeout') runs, then results include semantically related chunks even without exact term matches",
        "Given a query vector, when vector_search() runs, then it returns up to 200 candidates with cosine similarity scores from the Qdrant collection",
        "Given Qdrant is unreachable, when vector_search() runs, then it raises a retrieval error (caller handles degradation)"
      ],
      "dependencies": [7]
    },
    {
      "id": 10,
      "category": "core",
      "title": "Rank Fusion (RRF)",
      "description": "Implement RankFusion.fuse() using Reciprocal Rank Fusion with k=60, merging up to 5 ranked lists (4 primary + optional symbol boost) into a single top-50 candidate list",
      "priority": "high",
      "status": "failing",
      "verification_steps": [
        "Given 200 BM25 candidates and 200 vector candidates with 50 overlapping chunks, when fuse() runs with k=60, then overlapping chunks receive boosted RRF scores from both rankings and the output contains at most 50 candidates",
        "Given one empty result list and one non-empty list, when fuse() runs, then it returns results from the non-empty list only",
        "Given 4-way input (BM25 code, vector code, BM25 doc, vector doc), when fuse() runs, then all 4 lists are merged by RRF and execution completes within 10ms"
      ],
      "dependencies": [8, 9]
    },
    {
      "id": 11,
      "category": "core",
      "title": "Neural Reranking",
      "description": "Implement Reranker using bge-reranker-v2-m3 cross-encoder via sentence-transformers, scoring query-document pairs and selecting top-K results with fallback to RRF order on model failure",
      "priority": "high",
      "status": "failing",
      "verification_steps": [
        "Given 50 fused candidates and a query, when rerank() runs, then it returns top-6 candidates re-scored by the cross-encoder with relevance scores",
        "Given fewer than 3 candidates, when rerank() runs, then it returns all available candidates without error",
        "Given a reranker model failure (OOM, load error), when rerank() runs, then it falls back to the fusion-ranked order and logs a degradation warning"
      ],
      "dependencies": [10]
    },
    {
      "id": 12,
      "category": "core",
      "title": "Context Response Builder",
      "description": "Implement ResponseBuilder that assembles the dual-list response format: codeResults[] + docResults[] + rules{}, splitting reranked results by content_type, truncating content to 2000 chars",
      "priority": "high",
      "status": "failing",
      "verification_steps": [
        "Given 6 reranked results (3 code, 2 doc, 1 example), when build() runs, then the response contains codeResults with 3 items and docResults with 3 items (doc + example), each with content, repo, path, symbol, type, language, score fields",
        "Given a chunk with content exceeding 2000 characters, when build() runs, then the content is truncated to 2000 chars with a trailing '...' indicator",
        "Given a repository with rule chunks, when build() runs with include_rules=true, then the response includes a rules section with the full rule content"
      ],
      "dependencies": [11]
    },
    {
      "id": 13,
      "category": "core",
      "title": "Natural Language Query Handler",
      "description": "Implement QueryHandler.handle_nl_query() that runs the full hybrid pipeline: 4-way parallel retrieval, unified RRF fusion, single rerank, with query expansion for embedded code identifiers",
      "priority": "high",
      "status": "failing",
      "verification_steps": [
        "Given the NL query 'how to use grpc java interceptor', when handle_nl_query() runs, then it executes 4-way parallel retrieval (BM25 code + vector code + BM25 doc + vector doc), fuses with RRF, reranks, and returns structured context results",
        "Given an empty query string, when handle_nl_query() is called, then it raises a ValidationError with a descriptive message",
        "Given the NL query 'how does AuthService validate tokens', when query expansion runs, then it extracts 'AuthService' and fires a parallel symbol.raw term query merged into RRF with weight 0.3",
        "Given a pipeline that exceeds 1-second timeout, when handle_nl_query() runs, then it returns partial results with degraded=true flag"
      ],
      "dependencies": [12]
    },
    {
      "id": 14,
      "category": "core",
      "title": "Symbol Query Handler",
      "description": "Implement QueryHandler.handle_symbol_query() with auto-detection heuristic (dots, camelCase, snake_case, ::, #), ES term exact match → fuzzy fallback → NL pipeline fallback",
      "priority": "high",
      "status": "failing",
      "verification_steps": [
        "Given the query 'UserService.getById', when detect_query_type() runs, then it classifies as 'symbol' due to dot notation",
        "Given the symbol query 'std::vector', when handle_symbol_query() runs, then it first tries ES term query on symbol.raw, and returns matching class definitions and methods",
        "Given a symbol query for a non-existent symbol, when ES term and fuzzy queries return 0 results, then it falls back to the full NL pipeline using the query as-is",
        "Given a symbol query exceeding 200 characters, when submitted, then it raises a ValidationError"
      ],
      "dependencies": [12]
    },
    {
      "id": 15,
      "category": "core",
      "title": "Repository-Scoped Query",
      "description": "Implement repository filtering in Retriever that restricts BM25 and vector searches to chunks from a specified repo_id, applied as ES filter clause and Qdrant payload filter",
      "priority": "high",
      "status": "failing",
      "verification_steps": [
        "Given query 'timeout' with repo_filter='spring-framework', when retrieval runs, then all returned chunks have repo_id matching 'spring-framework'",
        "Given a repo_filter for a non-existent repository, when retrieval runs, then it returns an empty result set with 200 status",
        "Given a query without repo_filter, when retrieval runs, then it searches across all indexed repositories"
      ],
      "dependencies": [12]
    },
    {
      "id": 16,
      "category": "core",
      "title": "API Key Authentication",
      "description": "Implement AuthMiddleware for FastAPI with API key validation against PostgreSQL, Redis-cached lookups, rate limiting (10 failed attempts/IP/minute → 429), role-based access (read/admin), and API key CRUD (create/revoke/rotate/list)",
      "priority": "high",
      "status": "failing",
      "verification_steps": [
        "Given a valid API key in X-API-Key header, when a request is made, then the middleware allows the request and attaches the key's role to the request state",
        "Given an invalid or missing API key, when a request is made, then the middleware returns 401 Unauthorized without executing the handler",
        "Given more than 10 failed auth attempts from the same IP within 1 minute, when another attempt is made, then the middleware returns 429 Too Many Requests",
        "Given APIKeyManager.create_key(name='test', role='read'), when called, then it generates a key, stores the SHA-256 hash in PostgreSQL, and returns the plaintext key once"
      ],
      "dependencies": [2]
    },
    {
      "id": 17,
      "category": "core",
      "title": "REST API Endpoints",
      "description": "Implement FastAPI router with endpoints: POST /api/v1/query, GET /api/v1/repos, POST /api/v1/repos, POST /api/v1/repos/{repo_id}/reindex, GET /api/v1/health, and API key management endpoints",
      "priority": "high",
      "status": "failing",
      "verification_steps": [
        "Given a POST to /api/v1/query with valid query body and API key, when processed, then it returns 200 with structured context results (codeResults + docResults + rules)",
        "Given a GET to /api/v1/repos with valid API key, when processed, then it returns the list of registered repositories with their indexing status",
        "Given a GET to /api/v1/health without authentication, when processed, then it returns service health status with ES/Qdrant/Redis connectivity",
        "Given a malformed JSON body on POST /api/v1/query, when submitted, then it returns 400 with a validation error message"
      ],
      "dependencies": [13, 14, 15, 16]
    },
    {
      "id": 18,
      "category": "core",
      "title": "MCP Server",
      "description": "Implement standalone MCP server exposing 3 tools: search_code_context (with max_tokens param), list_repositories, get_chunk. Uses the same query handler as REST API.",
      "priority": "high",
      "status": "failing",
      "verification_steps": [
        "Given an MCP client calling search_code_context with {query: 'spring webclient timeout', repo: 'spring-framework'}, when executed, then it returns structured context results matching REST API format",
        "Given an MCP client calling list_repositories, when executed, then it returns the list of indexed repositories with status",
        "Given an MCP client calling search_code_context without the optional repo parameter, when executed, then it searches across all repositories",
        "Given an MCP tool call with missing required 'query' field, then it returns an MCP error response with a descriptive message"
      ],
      "dependencies": [13]
    },
    {
      "id": 19,
      "category": "ui",
      "title": "Web UI Search Page",
      "description": "Implement Jinja2 SSR web interface with search input, repository filter dropdown, language filter checkboxes, syntax-highlighted results with htmx partial updates. Developer Dark theme per UCD.",
      "priority": "medium",
      "status": "failing",
      "ui": true,
      "ui_entry": "/",
      "verification_steps": [
        "[devtools] Navigate / → EXPECT: search input field, repository filter dropdown, language filter checkboxes rendered with Developer Dark theme (bg #1a1b26, text #c0caf5); fill query 'timeout' → click Search → EXPECT: results displayed with syntax-highlighted code snippets, repo name, file path, symbol name, relevance score; REJECT: console errors, broken styles",
        "[devtools] Navigate / → submit empty query → EXPECT: validation message 'Please enter a search query'; REJECT: server error, blank results",
        "Given no results for a query, when displayed, then the UI shows a 'No results found' message with the Developer Dark empty state styling"
      ],
      "dependencies": [17]
    },
    {
      "id": 20,
      "category": "core",
      "title": "Language Filter",
      "description": "Implement LanguageFilter that adds ES/Qdrant filter clauses restricting results to specified programming languages, applied before retrieval",
      "priority": "medium",
      "status": "failing",
      "verification_steps": [
        "Given query 'timeout' with language_filter=['java'], when retrieval runs, then all returned code chunks have language='java'",
        "Given multiple language filters ['java', 'python'], when retrieval runs, then returned chunks are in either Java or Python",
        "Given an unrecognized language value 'rust', when submitted, then a ValidationError is raised listing supported languages",
        "Given an empty language filter list, when retrieval runs, then no language filtering is applied"
      ],
      "dependencies": [8, 9]
    },
    {
      "id": 21,
      "category": "core",
      "title": "Scheduled Index Refresh",
      "description": "Implement Celery Beat periodic task that triggers re-indexing for all registered repositories on a configurable cron schedule (default: weekly Sunday 02:00 UTC)",
      "priority": "high",
      "status": "failing",
      "verification_steps": [
        "Given the default cron configuration, when the scheduler fires, then it queues re-indexing jobs for all registered repositories with status='active'",
        "Given a scheduled job that fails, when the failure is detected, then the system retries once after 1 hour; if retry also fails, it logs an error and skips until next window",
        "Given a re-index already in progress for a repository, when the schedule fires, then it skips the duplicate and logs an informational message"
      ],
      "dependencies": [4]
    },
    {
      "id": 22,
      "category": "core",
      "title": "Manual Reindex Trigger",
      "description": "Implement POST /api/v1/repos/{repo_id}/reindex endpoint that queues an immediate re-indexing job for a specific repository, requiring admin role",
      "priority": "high",
      "status": "failing",
      "verification_steps": [
        "Given a POST to /api/v1/repos/{repo_id}/reindex with valid admin API key, when processed, then an indexing job is queued and the response contains job_id with status='queued'",
        "Given a reindex request for a non-existent repository, when processed, then it returns 404",
        "Given a reindex request with a read-only API key, when processed, then it returns 403 Forbidden"
      ],
      "dependencies": [17]
    },
    {
      "id": 23,
      "category": "operational",
      "title": "Metrics Endpoint",
      "description": "Implement /metrics endpoint using prometheus-client exposing query_latency_seconds, retrieval_latency_seconds, rerank_latency_seconds, index_size_chunks, queries_per_second, cache_hit_ratio",
      "priority": "medium",
      "status": "failing",
      "verification_steps": [
        "Given a GET to /metrics without authentication, when processed, then the response contains Prometheus text format with all required metric names: query_latency_seconds, retrieval_latency_seconds, rerank_latency_seconds, index_size_chunks, cache_hit_ratio",
        "Given several queries have been processed, when /metrics is called, then the histogram buckets for query_latency_seconds contain observed values"
      ],
      "dependencies": [17]
    },
    {
      "id": 24,
      "category": "operational",
      "title": "Query Logging",
      "description": "Implement structured JSON logging for query service: each query logs query, query_type, api_key_id, result_count, retrieval_ms, rerank_ms, total_ms, timestamp to stdout. Non-fatal.",
      "priority": "medium",
      "status": "failing",
      "verification_steps": [
        "Given a completed query, when logging executes, then a structured JSON log entry is written to stdout containing: query, query_type, result_count, retrieval_ms, rerank_ms, total_ms, timestamp",
        "Given a logging I/O failure, when it occurs during query processing, then the query response is not blocked or delayed"
      ],
      "dependencies": [13]
    },
    {
      "id": 25,
      "category": "core",
      "title": "Query Cache",
      "description": "Implement Cache with Redis backend and in-memory L1 cache. Cache key = hash(query + repo_filter + language_filter). TTL=300s. Cache invalidation on reindex.",
      "priority": "medium",
      "status": "failing",
      "verification_steps": [
        "Given a query that has been cached, when the same query is submitted again within TTL, then the cached result is returned without executing the retrieval pipeline",
        "Given a cached query result, when a repository reindex completes, then the cache entries for that repository are invalidated",
        "Given Redis is unavailable, when cache operations are attempted, then the system degrades gracefully to no-cache mode without errors"
      ],
      "dependencies": [13]
    },
    {
      "id": 26,
      "category": "non-functional",
      "title": "NFR-001: Query Latency p95 < 1s",
      "description": "Verify that p95 query latency is under 1000ms end-to-end under load (100 concurrent users)",
      "priority": "low",
      "status": "failing",
      "verification_steps": [
        "Given a Locust load test with 100 concurrent users sending diverse NL and symbol queries, when run for 5 minutes, then p95 end-to-end latency is < 1000ms"
      ],
      "dependencies": [25]
    },
    {
      "id": 27,
      "category": "non-functional",
      "title": "NFR-002: Query Throughput >= 1000 QPS",
      "description": "Verify sustained throughput of >= 1000 QPS with acceptable error rate",
      "priority": "low",
      "status": "failing",
      "verification_steps": [
        "Given a Locust load test ramping to 1000+ QPS, when sustained for 5 minutes, then the system maintains >= 1000 QPS with < 1% error rate"
      ],
      "dependencies": [26]
    },
    {
      "id": 28,
      "category": "non-functional",
      "title": "NFR-003: Repository Capacity",
      "description": "Verify the system can index and serve queries across 100-1000 repositories",
      "priority": "low",
      "status": "failing",
      "verification_steps": [
        "Given 100+ registered repositories with indexed content, when querying across all repos, then results are returned within the latency budget"
      ],
      "dependencies": [3]
    },
    {
      "id": 29,
      "category": "non-functional",
      "title": "NFR-004: Single Repository Size",
      "description": "Verify the system handles repositories up to 1GB in size",
      "priority": "low",
      "status": "failing",
      "verification_steps": [
        "Given a repository approaching 1GB, when indexing runs, then all files are processed and chunks are indexed without OOM or timeout"
      ],
      "dependencies": [4]
    },
    {
      "id": 30,
      "category": "non-functional",
      "title": "NFR-005: Service Availability 99.9%",
      "description": "Verify query service achieves 99.9% uptime with health monitoring",
      "priority": "low",
      "status": "failing",
      "verification_steps": [
        "Given the query service running with health check endpoint, when monitored over a test window, then uptime exceeds 99.9% with automatic recovery from transient failures"
      ],
      "dependencies": [17]
    },
    {
      "id": 31,
      "category": "non-functional",
      "title": "NFR-006: Linear Scalability >= 70%",
      "description": "Verify adding query nodes yields >= 70% of theoretical throughput increase",
      "priority": "low",
      "status": "failing",
      "verification_steps": [
        "Given N query nodes achieving X QPS, when deploying N+1 nodes, then throughput increases by >= 0.7 * (X/N)"
      ],
      "dependencies": [27]
    },
    {
      "id": 32,
      "category": "non-functional",
      "title": "NFR-007: Single-Node Failure Tolerance",
      "description": "Verify query service continues operating when any single node fails",
      "priority": "low",
      "status": "failing",
      "verification_steps": [
        "Given a multi-node query cluster under load, when one node is killed, then remaining nodes continue serving requests without failures"
      ],
      "dependencies": [30]
    }
  ]
}
